# 任务计划：梳理仓库并初始化 Agent Markdown

## 目标
完整理解仓库的用途、结构、生成链路与维护约束，在仓库根创建内容一致且可长期使用的 `AGENTS.md` 和 `CLAUDE.md`，并完成必要验证。

## 当前阶段
已完成

## 各阶段

### 阶段 1：恢复上下文与仓库清点
- [x] 归一化并核验当前路径
- [x] 检查 Git 状态、PWF 和现有 Agent Markdown
- [x] 建立完整的受控文件清单
- **状态：** complete

### 阶段 2：逐项理解仓库
- [x] 阅读根文档与工作流
- [x] 阅读资源和生成产物说明
- [x] 检查 Git 历史、远程配置和文件追踪关系
- [x] 将架构、维护方式与风险记录到 findings.md
- **状态：** complete

### 阶段 3：设计并初始化 Agent Markdown
- [x] 基于事实提炼仓库用途、结构和维护规则
- [x] 同步创建内容完全一致的 `AGENTS.md` / `CLAUDE.md`
- [x] 更新 `.gitignore`，落实本地配置与隐藏目录规则
- **状态：** complete

### 阶段 4：验证与交付
- [x] 校验两份 Agent Markdown 完全一致且 H1 正确
- [x] 校验忽略规则、Git 状态和文档描述准确性
- [x] 汇总仓库用途与所有改动
- **状态：** complete

### 阶段 5：记录进度并发布
- [x] 重新加载 PWF 并运行 session-catchup
- [x] 增量记录本轮发布请求与当前状态
- [x] 获取并核对远端 `main` 最新状态
- [x] 提交待发布变更并在需要时安全整合远端提交
- [x] 推送 `main` 并核验远端落点
- **状态：** complete

## 关键问题
1. 根 README 的内容、外部动态卡片与本地资源如何共同构成 GitHub 个人主页？
2. `profile-summary-card-output` 由哪个工作流生成，哪些文件应由 Agent 维护或避免手改？
3. 仓库中哪些隐藏目录需要作为 Git 跟踪例外？

## 已做决策
| 决策 | 理由 |
|------|------|
| 使用 PWF 三件套记录本次仓库级审阅 | 工作预计超过 5 次工具调用，且需要逐文件核查后再修改配置 |
| 不沿用现有 `CLAUDE.md` 的旧内容直接复制 | 其中本地路径已过时，H1 与全局同步约束不符，需根据仓库事实重建 |
| 用通配隐藏目录规则并显式反忽略 `.github/` | 同时覆盖未来本地工具目录，又保证工作流及新增文件继续可追踪 |
| 将旧 `CLAUDE.md` 从 Git 索引移除但保留本地文件 | 单靠 `.gitignore` 无法停止追踪已有文件；仓库根 Agent Markdown 必须本地维护、不进入 push |
| 发布前先 fetch 并比较本地/远端 | 仓库每日有 bot 提交，避免基于过时的 `origin/main` 直接 push |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| ImageMagick `identify` 未安装 | 1 | 不安装新依赖，改用 `file`、SVG 根属性、`xmllint` 和现有图片查看工具 |
| 图片查看工具无法直接处理 5 个动态 SVG | 1 | 已完成源码与 XML 校验；若现有本地渲染器可用，则转换代表帧后检查，不重复直接打开 |
| 首次主题引用计数使用 `grep -c`，统计的是匹配行而非同一行内的全部出现次数 | 1 | 改用 `grep -Fo ... | wc -l` 重新核验实际引用数 |

## 备注
- 仓库外部或网络内容只写入 `findings.md`，不进入本计划。
- PWF 三件套必须保持可追踪，不得加入 `.gitignore`。
